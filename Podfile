platform :ios, '12.0'

target 'pruebaMeep' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  #use_frameworks!
  
  #Librería para las llamadas a servicio
  pod 'Alamofire', '~> 5.1.0'
  
  #Future con compatibilidad desde iOS 10 < *
  pod 'PromisedFuture'
  
  #Librería de google maps
  pod 'GoogleMaps' 
  
end
